<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <com.handmark.pulltorefresh.library.PullToRefreshListView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/lv_fragment_home">
    </com.handmark.pulltorefresh.library.PullToRefreshListView>
    <ImageView
        android:id="@+id/iv_loading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_alignLeft="@+id/tv_loading"
        android:layout_alignRight="@+id/tv_loading"
        android:src="@drawable/anim_list_loading"/>
    <TextView
        android:id="@+id/tv_loading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/iv_loading"
        android:layout_centerHorizontal="true"
        android:textColor="@color/home_bottom_text_checked_color"
        android:text="loading..."/>

</RelativeLayout>